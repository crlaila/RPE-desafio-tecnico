import styled from "styled-components";

const Container = styled.div`
  background-color: #191919;
  color: white;
  padding: 20px;
`;

export function Navbar() {
  return <Container>Seja bem-vindo(a) a nossa loja!</Container>;
}

export default Navbar;
